
			<%= form_for(@project) do |f| %>

			<%= f.label(:title) %>
			<%= f.text_field(:title) %>

			<%= f.label(:description) %>
			<%= f.text_field(:description) %>

			<%= f.label(:start_date) %>
			<%= f.datetime_select(:start_date) %>

		<%= f.submit %>

		<% end %>
