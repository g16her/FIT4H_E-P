<p id="notice"><%= notice %></p>

<h1>Listing Employees</h1>

<table>
  <thead>
    <tr>
      <th>Vorname</th>
      <th>Nachname</th>
      <th>Username</th>
      <th>Project</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @employees.each do |employee| %>
      <tr>
        <td><%= employee.vorname %></td>
        <td><%= employee.nachname %></td>
        <td><%= employee.namekomplett %></td>
        <td><%= employee.project_id %></td> <!-- project_id -->
        <td><%= link_to 'show', project_employee_path(@project,employee) %> </td> <!-- Links anpassen -->
        <td><%#= link_to 'edit', edit_project_employee_path(@project,employee) %> </td>
        <!-- Links anpassen (FEHLER) --> 
       
        <td><%#= link_to 'Destroy', employee, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%#= link_to 'New Employee', new_employee_path %>
